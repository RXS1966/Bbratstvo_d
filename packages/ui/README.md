# @repo/ui — UI-kit

Компоненты: `Button`, `Input`, `Label`, `Card`, `Badge`, `Alert`, `cn`.

Использование в приложении:

```tsx
import { Button, Card, CardHeader, CardTitle } from "@repo/ui";
```

Новые компоненты — в `src/components/`, реэкспорт в `src/index.ts`.
